package net.matsulen.lkartifacts.item.custom;

import java.util.UUID;

public interface DistanceItem {
    UUID BLOCK_REACH_MODIFIER = UUID.randomUUID();
    UUID ENTITY_RANGE_MODIFIER = UUID.randomUUID();
}
